services:
  web:
    build: .
    ports:
      - "5173:5173"
    command: sh -c "npm ci && npm run dev -- --host 0.0.0.0 --port 5173"
    volumes:
      - .:/app
      - node_modules:/app/node_modules

volumes:
  node_modules:
